let mongoose;_cf8‍.x([["default",()=>_cf8‍.o]]);_cf8‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const { Schema } = mongoose;

const PostSchma = new Schema({
  title : String,
  body : String,
  tags: [String],
  publishedDate: {
    type : Date,
    default : Date.now
  }
});

const Post = mongoose.model('Post', PostSchma);
_cf8‍.d(Post);