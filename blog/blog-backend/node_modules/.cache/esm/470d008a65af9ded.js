let Koa,Router,bodyParser,mongoose,api,jwtMiddleware;_65a‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_65a‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_65a‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_65a‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_65a‍.w("./api",[["default",["api"],function(v){api=v}]]);_65a‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);require('dotenv').config();








const { PORT, MONGO_URI } = process.env;

mongoose
.connect(MONGO_URI)
  .then(() => {
    console.log('Connected to MongoDB');
  })
  .catch(e => {
    _65a‍.g.console.error(e);
});

const app = new Koa();
const router = new Router();

router.use('/api', api.routes());

app.use(bodyParser());
app.use(jwtMiddleware);

app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;
app.listen(4000, () => {
  _65a‍.g.console.log('Listening to port %d', port);
})